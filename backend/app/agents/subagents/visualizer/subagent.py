"""The visualizer subagent's deep-agents config: name, description, prompt, and its one tool
(select_chart). Reads rows the SQL subagent already wrote to the shared file rather than
re-querying — the same rule the old visualizer_node's re-chart path in agent.py followed.
"""
