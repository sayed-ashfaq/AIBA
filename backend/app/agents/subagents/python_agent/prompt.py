"""System prompt for the Python agent: what data is available (the shared result file), which
libraries are importable in the sandbox, and the expectation that output goes back to a file rather
than as raw text, so downstream subagents (visualizer) can pick it up the same way.
"""
